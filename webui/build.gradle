plugins {
    id "java"
    id "project.defaults"
    id "base.application"
    alias libs.plugins.classTokenReplacer
}

dependencies {
    include project(":core")
    include libs.commons.gson
    include libs.gson
}
